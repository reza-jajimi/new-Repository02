import { Box, Typography } from "@mui/material";
import React from "react";

const page = () => {
  return (
    <Box>
      <Typography variant="h4">salam</Typography>
    </Box>
  );
};

export default page;
